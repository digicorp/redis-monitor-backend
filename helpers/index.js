const index = {
    modules: require('./npmModules'),
    common: require('./common'),
    globalMessages: require('./globalMessages')
    // redisClient: require('./init_redis')
}

module.exports = index;