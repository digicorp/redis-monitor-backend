module.exports = {
    express: require('express'),
    path: require('path'),
    cors: require('cors'),
    redis: require('redis'),
    timeout: require('connect-timeout'),
    fs: require('fs'),
}